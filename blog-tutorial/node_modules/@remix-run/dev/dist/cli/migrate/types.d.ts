import type { Flags } from "./flags";
export declare type MigrationFunction = (projectDir: string, flags?: Flags) => Promise<void>;
export interface Migration {
    id: string;
    description: string;
    function: MigrationFunction;
}
